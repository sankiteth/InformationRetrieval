How to run:

1. add tinyir.jar to the folder lib/
2. add to src/main/resources/docs the zip with the dataset
3. run on the command line:
	$ sbt run

the code will produce two files, the one indexed with 0 is for the term-model, the one indexed with 1 is for the language model.